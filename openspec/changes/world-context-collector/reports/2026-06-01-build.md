# Build Report — world-context-collector

**Date:** 2026-06-01  
**Branch:** feature/world-context-collector

## Result

```
.\gradlew.bat compileJava compileClientJava --offline → BUILD SUCCESSFUL (20s)
```

Main y client sourceset compilados sin errores.

## Archivos creados/modificados

| Archivo | Operación |
|---------|-----------|
| `context/WorldContext.java` | NUEVO — record con 6 sub-records |
| `context/WorldContextCollector.java` | NUEVO — 4 métodos de escaneo |
| `http/ButlerHttpClient.java` | MODIFICADO — firmas con WorldContext, serialización manual |
| `command/ButlerCommand.java` | MODIFICADO — WorldContextCollector en /butler ask |
| `client/voice/VoiceKeyBinding.java` | MODIFICADO — WorldContextCollector en doSend |

## Advertencias

- `AIButlerClient.java` emite aviso de API deprecada (preexistente, no introducida en este change).

## Notas

- `--offline` requerido por SNAPSHOT de Loom (entorno sin acceso a maven.fabricmc.net).
- Se usó `Container.getItem(i)` para el inventario del jugador (alternativa segura a `inv.items`).
