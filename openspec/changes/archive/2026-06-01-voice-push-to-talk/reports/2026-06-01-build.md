# Build Report — voice-push-to-talk

**Date:** 2026-06-01  
**Change:** voice-push-to-talk  
**Branch:** feature/voice-push-to-talk

## Compilation Result

```
.\gradlew.bat compileJava --offline    → BUILD SUCCESSFUL
.\gradlew.bat compileClientJava --offline → BUILD SUCCESSFUL
```

Both sourcesets compiled without errors.

## API Adaptations Required (vs Technical Plan)

The technical plan was written against a slightly different API version. Two corrections were needed:

| Plan | Actual (Fabric API 0.149.1+26.1.2) |
|------|--------------------------------------|
| `net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper` | `net.fabricmc.fabric.api.client.keymapping.v1.KeyMappingHelper` |
| `KeyBindingHelper.registerKeyBinding(...)` | `KeyMappingHelper.registerKeyMapping(...)` |
| `"key.categories.gameplay"` (String) | `KeyMapping.Category.GAMEPLAY` (Category record constant) |
| `mc.gui.getChat().addMessage(Component)` | `mc.gui.getChat().addClientSystemMessage(Component)` |

## Files Created/Modified

- `src/main/java/com/miguealguacil/butler/http/ButlerHttpClient.java` — added `ServerException.status()` getter, `sendVoiceAsync()`, `askVoiceAsync()`, `parseActions()` helper
- `src/client/java/com/miguealguacil/butler/client/voice/VoiceRecorder.java` — new
- `src/client/java/com/miguealguacil/butler/client/voice/VoiceKeyBinding.java` — new
- `src/client/java/com/miguealguacil/butler/client/AIButlerClient.java` — added `VoiceKeyBinding.register()`
- `src/main/resources/assets/ai-butler/lang/en_us.json` — new (key translation)

## Deprecation Warning (pre-existing)

`AIButlerClient.java` emits a deprecation warning for `EntityRendererRegistry.register(...)`. This is pre-existing code and unrelated to this change.

## In-Game Verification

Requires user to:
1. Start FastAPI backend: `cd <backend> && uv run uvicorn app.main:app --reload`
2. Run: `.\gradlew.bat runClient --offline`
3. Open a world, press `V`, speak ~2s, release — verify `[Alfred] Grabando...` on press and Alfred's response on release
4. Press+release `V` quickly — verify `[Alfred] Audio demasiado corto.`
5. Open inventory (E), press `V` — verify no recording starts
