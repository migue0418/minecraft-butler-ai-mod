# Build report — world-context-mobs

**Date:** 2026-06-02  
**Command:** `.\gradlew.bat compileJava --offline`  
**Result:** BUILD SUCCESSFUL (10s)

## Changes compiled

- `WorldContext.java` — `NearbyContext` record añade campo `monsters: List<AnimalGroup>`
- `WorldContextCollector.java` — eliminado `isLoaded` check; añadido `collectMonsters`; AABB ajustado a ±25/±5; `collect()` actualizado
- `ButlerHttpClient.java` — serialización de `nearby.monsters` en `worldContextToJson`
